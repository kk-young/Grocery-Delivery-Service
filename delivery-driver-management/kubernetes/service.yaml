apiVersion: v1
kind: Service
metadata:
  name: delivery-driver-management
  labels:
    app: delivery-driver-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: delivery-driver-management